import Intro from 'features/home/intro.mdx';
import Bio from 'features/home/bio.mdx';
import AboutMe from 'features/home/about_me.mdx';
import { ProfileCard } from 'features/home/ProfileCard';
import { SocialMediaList } from 'features/home/components/SocialMediaList';
import { socialMedia } from 'configs/SocialMedia';

<div className='hidden lg:flex p-9 gap-12 items-center'>
  <div>
    <Intro />
    I'm a developer specializing in both **mobile app and web development**.
    <AboutMe />
  </div>
  <ProfileCard />
</div>
<div className='lg:hidden p-9 items-center'>
  <Intro />
  <Bio />
  <AboutMe />
  <br />
  <div className='flex flex-col items-center'>
    Connect with me on
    <SocialMediaList data={socialMedia} />
  </div>
</div>

<style
  jsx
  global
>{`
  .dark body {
    background: var(--dark-theme);
    background: radial-gradient(ellipse at center, var(--secondary-dark-theme) 0%, var(--bg-dark) 70%);
  }
  .light body {
    background: var(--light-theme);
    background: radial-gradient(ellipse at center, var(--secondary-light-theme) 0%, var(--bg-light) 70%);
  }
`}</style>
